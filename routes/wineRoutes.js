const { songs } = require('../controllers')

module.exports = app => {
  // Create a new song
  app.post('/', (req, res) => {
    
    })
  })

  // Toggle whether the song has been listened to
  app.put('/', (req, res) => {
    
    })
  })

  // Get all songs
  app.get('/', (req, res) => {
    
    })
  })
}
